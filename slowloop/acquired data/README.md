`post_transition.csv` has data acquired by telemetry_daemon.py

`py_monitor_ack.csv` has ACK Turnaround time recorded by python script and virtual monitor (Approach 1, ACK Variance)

`ack_delays_tcpdump.csv` has ACK Truenaround time delays recorded through tcpdump (Approach 2, ACK Variance)

`good_connection.csv` has packet data for client with good connection decoded through Wireshark (Approach 3, ACK Variance)

`bad_connection_1.csv` has packet data for client with bad connection (Observation 1) decoded through Wireshark (Approach 3, ACK Variance)

`bad_connection_2.csv` has packet data for client with bad connection (Observation 2) decoded through Wireshark (Approach 3, ACK Variance)
